{
  "devDependencies": {
    "eslint-config-readable": "^1.10.2",
    "lerna": "^2.9.1"
  },
  "scripts": {
    "install": "lerna bootstrap",
    "publish": "lerna publish",
    "build": "lerna run build",
    "test": "npm run build"
  }
}
